"""Placeholder so pytest has something to collect while real tests are backlogged.

Delete once there are genuine tests — its only job is to keep CI green.
"""


def test_placeholder() -> None:
    assert True
